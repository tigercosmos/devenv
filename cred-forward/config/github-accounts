# Managed by devenv cred-forward.
# Maps a GitHub repository owner to the local gh login that the forwarded
# gh wrapper uses for it. One "OWNER ACCOUNT" pair per line; "*" is the
# fallback for every other owner. Owners match case-insensitively.
# Leave this file out to use the active local gh login for everything.
t2-auto anchi-t2
solvcon tigercosmos
tigercosmos tigercosmos
* tigercosmos
