#!/usr/bin/env bash
# devenv-update — kept for existing habits; the command is `devenv update`.
exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}" 2>/dev/null || echo "${BASH_SOURCE[0]}")")/devenv" update "$@"
