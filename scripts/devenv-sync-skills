#!/usr/bin/env bash
# devenv-sync-skills — kept for existing habits; the command is `devenv sync-skills`.
exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}" 2>/dev/null || echo "${BASH_SOURCE[0]}")")/devenv" sync-skills "$@"
