#!/usr/bin/env bash
# devenv-doctor — kept for existing habits; the command is `devenv doctor`.
exec "$(dirname "$(readlink -f "${BASH_SOURCE[0]}" 2>/dev/null || echo "${BASH_SOURCE[0]}")")/devenv" doctor "$@"
